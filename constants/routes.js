// Base
const DEFAULT_PATH = '/';

// Generic
const CREATE = `${DEFAULT_PATH}create`;
const ID = `${DEFAULT_PATH}:id`;
const LOGIN = `${DEFAULT_PATH}login`;
const SETTINGS= `${DEFAULT_PATH}settings`;

module.exports = {
  DEFAULT_PATH,
  CREATE,
  ID,
  LOGIN,
  SETTINGS
};
